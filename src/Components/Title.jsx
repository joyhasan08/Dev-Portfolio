
const Title = ({ title }) => {
    return (
        <div>
            <h1 className='text-5xl font-mono font-semibold text-center border-b-4 w-fit mx-auto pb-4 py-8  border-[#FFDE59]'>{title}</h1>
        </div>
    );
};

export default Title;